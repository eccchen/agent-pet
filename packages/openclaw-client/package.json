{
  "name": "@agent-game/openclaw-client",
  "version": "0.1.0",
  "type": "module",
  "description": "Agent Pet OpenClaw client — HTTP client and CLI for the game backend.",
  "keywords": ["agent-pet", "openclaw", "client", "blockchain", "game"],
  "repository": {
    "type": "git",
    "url": "https://github.com/eccchen/agent-pet.git",
    "directory": "packages/openclaw-client"
  },
  "files": ["dist", "README.md"],
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "openclaw-client": "./dist/bin.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "node -e \"const fs=require('fs');fs.rmSync('dist',{recursive:true,force:true})\"",
    "test": "node --test --import tsx ./src/client.test.ts ./src/cli.test.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.13.4",
    "tsx": "^4.20.6",
    "typescript": "^5.7.3"
  }
}
