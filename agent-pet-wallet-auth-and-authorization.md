# Agent 宠物区块链游戏钱包接入与授权方案

## 1. 文档目标

这份文档用于明确第一版的钱包接入、账户认证和宠物预算授权方案。

当前已确认前提：
- 用户通过 OpenClaw 或类似 Agent 工具接入游戏。
- 用户使用 OKX `Agentic Wallet` 登录。
- Agentic Wallet 支持邮箱登录，并能获得真实链上地址。
- 当前目标链为 `X Layer testnet`。

本方案的核心目标是：

`让用户用 Agentic Wallet 作为唯一链上身份和资产控制入口，让宠物在不拥有独立财权的前提下，使用受限预算参与游戏经济。`

## 2. 核心结论

### 2.1 玩家账户身份
玩家账户身份直接使用：

`Agentic Wallet 地址`

它同时承担：
- 游戏账户唯一标识
- 资产控制地址
- 结算地址
- 宠物预算授权的来源地址

### 2.2 认证方式
首版推荐采用两层认证模型：

#### 模式 A：标准消息签名认证
如果 Agentic Wallet 当前环境支持消息签名，则采用：
- 钱包地址获取
- 登录挑战 nonce
- 钱包签名 challenge
- 后端验签
- 创建游戏 session

#### 模式 B：已连接钱包会话认证
如果当前 Agentic Wallet 环境更稳定的是“交易签名/合约调用”而非通用离线消息签名，则首版可以采用：
- 用户在 OpenClaw 中登录 Agentic Wallet
- 读取当前地址
- 通过钱包连接状态建立受控 session
- 使用后续一次轻量授权动作完成账户归属确认

首版建议：

`优先支持模式 A，模式 B 作为兼容兜底。`

## 3. 为什么不需要私钥导出

本项目不需要用户导出私钥。

原因：
- 游戏需要的是地址控制权证明，不是私钥明文。
- 钱包能完成签名、交易和合约调用即可。
- 钱包不导出私钥，反而更符合安全边界。
- 你们不应接触、保存或请求用户私钥。

对游戏来说，真正重要的是：
- 能拿到地址
- 能证明地址归用户控制
- 能对大额链上动作确认

## 4. 首版接入流程

## 4.1 首次接入流程

1. 用户安装并打开你们的游戏 skill。
2. 用户在 OpenClaw 中登录 OKX Agentic Wallet。
3. 系统读取当前钱包地址。
4. 系统检测链环境，要求切到 `X Layer testnet`。
5. 用户完成一次账户归属认证。
6. 系统创建 Player 账户并绑定钱包地址。
7. 用户领养第一只宠物。

## 4.2 账户归属认证流程

### 首选方案：消息签名
1. 后端生成一次性 challenge nonce。
2. 钱包对 challenge 进行签名。
3. 后端验证签名归属该地址。
4. 生成登录 session。

### 兼容方案：轻量链上确认
如果通用消息签名不可用，可使用一次轻量链上授权动作作为归属确认，例如：
- 调用一个无资金风险的注册函数
- 记录玩家地址与游戏账户绑定

要求：
- 不涉及高额 gas
- 不涉及资金转移
- 仅作为账户绑定确认

## 5. 钱包与宠物的权限模型

## 5.1 所有权原则
- 所有资产最终所有权归玩家。
- 宠物不是独立钱包主体。
- 宠物不能导出、持有或转移玩家主账户控制权。

## 5.2 使用权原则
宠物只拥有：
- 指定预算范围内的可执行权限
- 特定动作范围内的支出权限
- 大额动作的申请权

## 5.3 三层权限结构

### 第一层：玩家主控
玩家控制：
- 钱包登录
- 钱包连接
- 充值和提现
- 高额资产动作
- 大额押注
- 收养和赎身确认

### 第二层：宠物预算权限
宠物可在预算范围内执行：
- 小额约架
- 小额悬赏
- 小额打赏
- 低风险社交支出

### 第三层：高敏动作申请
宠物可发起但不能自动执行：
- 中高额押注
- 中高额悬赏
- 高价值合约调用
- 收养/赎身相关动作

## 6. 预算授权设计

## 6.1 授权目标
预算授权的目的是：
- 让宠物能活跃行动
- 不让宠物随意花大钱
- 不让用户每个小动作都重复确认

## 6.2 首版授权参数
建议首版对每只宠物设置：
- `daily_limit`
- `single_tx_limit`
- `allowed_action_types`

### 建议默认值
- 每日预算上限：`200 Pet Credit`
- 单次自动支出上限：`50 Pet Credit`
- 可自动执行动作：
  - 小额约架
  - 小额打赏
  - 普通社交动作
  - 基础训练

### 需要额外确认的动作
- 超出单次自动支出上限
- 超出每日预算
- 涉及高风险合约调用
- 涉及收养/赎身

## 6.3 预算授权生命周期

1. 玩家创建或修改宠物预算规则。
2. 预算规则同步到产品层和结算层。
3. 宠物行动前检查是否符合授权。
4. 符合则自动执行。
5. 不符合则进入待确认状态。

## 7. 交易动作分级

## 7.1 自动执行动作
满足以下条件时可自动执行：
- 金额低于单次上限
- 动作类型在白名单中
- 未超过日预算
- 不涉及资产所有权转移

首版自动动作建议包括：
- 普通社交动作
- 小额打赏
- 小额约架报名
- 基础训练

## 7.2 二次确认动作
满足以下任一条件时必须确认：
- 超出单次预算
- 涉及锁仓
- 涉及中高额押注
- 涉及中高额悬赏
- 涉及特殊资产使用

## 7.3 禁止自动动作
首版一律禁止自动：
- 提现
- 收养成交
- 赎身成交
- 高额转账
- 非白名单合约调用

## 8. X Layer testnet 上链动作范围

首版建议上链动作如下：

### 必须上链
- 玩家账户与链地址绑定确认
- 预算授权相关结算凭证
- 约架押注锁仓和结算
- 悬赏锁仓和结算
- 打赏实际转账
- 收养/赎身结算

### 暂不强制上链
- 宠物人格变化
- 关系变化
- 社交热度
- 文案内容
- 行为动机

## 9. 收益池与提取授权

## 9.1 收益池性质
收益池是产品层视图，不代表宠物拥有独立钱包。

其作用是：
- 记录宠物近期创造的价值
- 给玩家提供提取或复投决策
- 给人格反馈提供依据

## 9.2 提取逻辑
玩家可以从收益池中提取资产回主控账户。

提取动作必须：
- 由玩家触发或确认
- 记录到事件系统
- 回流影响宠物忠诚、怨气和行为倾向

## 10. 风险与安全边界

## 10.1 首版安全底线
- 不请求用户私钥
- 不保存用户私钥
- 不让宠物成为独立热钱包
- 不允许高额动作自动广播

## 10.2 关键风险点
- 用户误以为宠物拥有完全花钱权限
- 钱包连接状态丢失导致 session 异常
- 大额动作误触发
- 外部 Agent 环境行为不稳定

## 10.3 应对建议
- 所有高敏操作都显示“玩家确认”
- 所有自动动作都展示预算来源和剩余额度
- 所有链上动作都展示目标链为 `X Layer testnet`
- 首版保留清晰的执行日志

## 11. 产品文案建议

为了让用户理解边界，建议首版统一使用以下叙述方式：

- “你的钱包是唯一资产控制地址。”
- “宠物只能在你授权的预算内行动。”
- “大额操作需要你确认。”
- “宠物赚到的钱仍然属于你，但你的处理方式会影响它的态度。”

## 12. 对开发的直接要求

首版开发必须满足：
- 钱包地址能被稳定读取
- 玩家地址能完成账户绑定
- 预算授权规则可配置
- 自动动作和手动确认动作可明确区分
- 所有高风险动作均有审计日志

## 13. 当前结论

第一版钱包方案可以正式定成：

`玩家使用 OpenClaw + OKX Agentic Wallet 接入，以钱包地址作为唯一链上身份；玩家通过签名或等效确认完成账户绑定；宠物在玩家授权预算内自动行动，超过阈值的一切高敏动作均由玩家确认；所有链上经济行为优先在 X Layer testnet 上验证。`

这套方案已经足够支持你们进入账户接入、预算授权和合约结算开发。
