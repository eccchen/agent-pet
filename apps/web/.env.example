# ── 必填（生产环境）─────────────────────────────────────────────────────────────
# 游戏服务器地址，前端通过此地址请求数据（必须以 NEXT_PUBLIC_ 开头才能在浏览器里生效）
NEXT_PUBLIC_API_BASE_URL=https://api.yourdomain.com

# ── 本地开发默认值（无需修改）────────────────────────────────────────────────────
# 不设置时自动 fallback 到 http://localhost:3001
